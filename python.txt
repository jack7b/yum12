# python的介绍
# python的发展史
# 荷兰 龟叔
# 为教学开发了一门语言ABC ，不开源
# 1989年的圣诞节 python 开源
# 做科学计算的工具 Facebook torch 针对python的版本开发了pytorch 2017年
# 2. python的版本
# python2x
# python3x
# 2版本和3版本完全不兼容
# 3.python 的特点
# 优点：
# （1） python的语法简洁
# （2） python可以支持面向过程和面向对象两种变成规范
# （3） 解释执行语言，可以跨平台，移植性
# （4） 可扩展性强
# （5） 强大丰富的支持库
# 缺点：

# （1） 执行速度慢： python的底层是c语言写的 for
# （2） 代码无法加密
# （3）
# 二、python 的开发环境
# 在windows下安装注意事项：
"""
1. 安装3.6版本
2. 勾选add path，自定义安装
3. 选择自定义安装路径（记住文件夹的位置）
4. 测试python是否安装成功
到控制台 执行python命令，如果出现交互模式（三个箭头）

print("hello world")
"""

"""
安装pycharm的注意事项
1. 自定义安装路径
2. pycharm的破解2017.3.3

"""



# 三 python的执行
# 两种形式：
#（1）交互式： 在控制台输入python命令，等待交互执行
#（2）脚本式：
# 脚本：源文件
# python代码全部写在.py文件中。python解释器会执行整个py文件
# 注意python的交互模式和脚本模式不能混合使用。
# 注意后台路径跟文件路径保持一致。

#开发代码习惯
#1不允许使用中文名称 拼音
 
第一章 python入门


# 四、变量和常量
print("hello word")
print("hello world")
# 为了方便调用，采用变量来引用实际内容
# 格式： 变量名 =变量值 注意：=不是等于 ，是赋值的意思
# 使用变量名，绑定变量值
# 将变量值赋予给变量名
# 简单的看起来：给变量值起名字，或者贴一个标签
# a="hello world!"
# print(a*5) 就会出现5个 hello world！
# 注意不同的语言对于变量的定义实际形式的不同。
# 3.变量定义时的内存结构
# （1） 在内存中先创建一片内存存储hello字符串
# （2） 在内存中创建一片内存存储a
# （3） 使用变量名绑定变量
#  对于变量来说，可以使用多个变量名绑定同一个变量值
#  注意：对于数值、字符串这些基本数据类型来说，在内存中只

# 4.修改变量的绑定
# 一个变量名不能同时绑定两个变量值
#内存中会先跟原来的变量值进行解绑，在绑定新的变量值
# 一个变量名可以绑定任何一种数据类型
#一个变量名可以绑定任何一种数据类型，叫做鸭子类型语言
a="hello"
a=123
print(a)
# 5.使用变量名绑定其他变量名
# 使用变量名绑定其它变量名，不能直接绑定该变量名，只能绑定该变量名所指向的值（所指向的对象）
# 6变量名的定义
# 注意：必须使用大小写英文字母，数字和_组合，不能以数字开头
# 中文名称可以当做变量名，但是建议不要这样做
# 变量在使用之前必须先赋值，赋值之后的变量才会在内存中创建。

#变量名不能是关键字
# 练习：定义一个x变量名，绑定"abc"变量值。使用y变量名绑定x变量名，将x修改绑定，>绑定到123上

# 7 变量赋值的简化
# （1）多个变量名指向同一个变量值
  a=1 b=1 c=1
#简化
#  a=b=c=1
# print(a)
# print(b)
# pring(c)
# （2）平行赋值
x=1 y=2
x,y=y,x
print(x)
print(y)

# 五、注释
# 在代码前加# 会对当前行的代码进行注释
# 对复杂的代码进行注释
# 从# 开始一直到当前的行末端都会注释掉，被注释掉的内容不会被python解释器解析


# 六、输入输出
# 1、输出
# 语法 print(输出的内容）
# pritn 内建模块 功能：在后台输出打印内容 点击安装Ctrl pycharm中出现print就可以看到内建模块的源码。
# print("hello world")
# print("hello world","python",123)
# 2 输入input 通过键盘输入
# 研发：input（”提示的内容")
# # 入门游戏：猜数字
#realvalue=8#设置数字的真实值

#
#注意：
(1) int 函数能够讲过字符串变数字
(2) 判断内容是否相等要使用 ==
(3)  注意缩进

